query Bill($id: ID!) {
  bills(billId: $id) {
    id,
    title,
    congress,
    sponsor {
      party,
      state,
      district,
      person {
        lastname,
        firstname
      }
    }
  }
}